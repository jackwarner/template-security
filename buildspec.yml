version:  0.1

phases:
  install:
    commands:
      # DO NOT MODIFY!!!
      - npm install -g serverless@1.5.0 2>&1 >/dev/null
  build:
    commands:
      # TODO: Update my.new.service to match the name of your service
      # NOTE: Should exactly match the name of your app repository (not the security repository), and use a delimiter of '.' not '-'
      # In Other words, leave '.security' off the end :)
      - SLS_DEBUG=true sls deploy --stage $STAGE --serviceName my.new.service --region us-east-1
  post_build:
    commands:
      # Remove pipelines
      # USE WITH EXTREME CAUTION!
      # Read and understand https://gamestop.atlassian.net/wiki/x/R4CAAw before proceeding:
      #- SLS_DEBUG=true sls remove --stage $STAGE --serviceName my.new.service --region us-east-1
